name = "python"
purpose = "Python importer: extract file→file import edges via tree-sitter (bundled WASM grammar) + module→file resolution through ownership. One language implementation of the Importer contract, which lives in the crossings cell. Adding a language = a sibling cell like this one, registered in importers' DEFAULT_IMPORTERS."
provides = ["pythonImporter", "fileToModule"]
requires = ["crossings", "ownership"]
