name = "validate"
purpose = "Check partition integrity: single-valued, dangling, orphan, undeclared-cell, unknown-require, and membrane drift (stale provides — declared but no owned file references them)."
provides = [
  "validatePartition",
  "staleProvidesOf",
  "isUnsafePath",
  "Violation",
  "StaleProvide",
]
requires = ["ownership", "declaration"]

layer = 2
