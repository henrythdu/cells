name = "assign"
purpose = "Mutate the partition: assign/reassign file ownership between cells, or remove files to orphan (the write side of the workflow)."
provides = ["assignFiles", "unassignFiles"]
requires = ["ownership"]

layer = 1
