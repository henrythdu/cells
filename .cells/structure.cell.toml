name = "structure"
purpose = "Graph analysis on the crossing graph: governance (ADP cycles, layer Direction — deps point toward core, SDP — deps run toward stability) + cycle-cut suggestions + layer inference, and change-impact (blast radius). Pure."
provides = [
  "detectCycles",
  "checkDirection",
  "checkSDP",
  "cycleCutCandidates",
  "formatStructureReport",
  "formatSdpReport",
  "formatLayerOverview",
  "formatLayerSuggestions",
  "computeImpact",
  "formatImpactReport",
  "formatStructureSummary",
  "classifyChangeCoupling",
  "formatChangeCouplingReport",
]
requires = ["crossings", "declaration", "config", "ownership"]
layer = 3
