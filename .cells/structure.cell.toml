name = "structure"
purpose = "Graph analysis on the crossing graph: governance (ADP cycle detection + layer Direction — deps point toward core/layer 0) and change-impact (blast radius). Pure."
provides = [
  "detectCycles",
  "checkDirection",
  "formatStructureReport",
  "formatLayerOverview",
  "computeImpact",
  "formatImpactReport",
]
requires = ["crossings", "declaration"]

layer = 1
