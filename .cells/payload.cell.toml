name = "payload"
purpose = "Assemble a cell's completeness payload (declaration + owned code + neighbor membranes) into one markdown document."
provides = ["assemblePayload"]
requires = ["declaration"]
