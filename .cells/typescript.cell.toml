name = "typescript"
purpose = "TS/JS/TSX importer specs (AST → specifiers → the ts-resolution core)"
provides = ["typescriptImporter", "tsxImporter", "javascriptImporter"]
requires = ["imports", "tree-sitter", "ts-resolution"]
layer = 4
