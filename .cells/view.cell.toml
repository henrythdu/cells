name = "view"
purpose = "Format the partition for human navigation: cell list + single-cell detail."
provides = ["formatCellList", "formatCellShow", "formatSizeReport"]
requires = ["declaration", "ownership", "crossings", "payload"]

layer = 1
