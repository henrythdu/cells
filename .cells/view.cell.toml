name = "view"
purpose = "Format the partition for human navigation: cell list + single-cell detail."
provides = ["formatCellList", "formatCellShow"]
requires = ["declaration", "ownership", "crossings"]

layer = "rule"
