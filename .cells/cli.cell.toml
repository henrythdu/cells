name = "cli"
purpose = "CLI entry: dispatch commands; wire the io layer + logic cells to argv. Owns the mutation command bodies (init/rename/remove/assign/unassign/new/prune-stale/plan in mutate.ts); read/analysis commands delegate to the commands cell."
provides = ["main", "cmdInit", "cmdRename", "cmdRemove", "cmdAssign", "cmdUnassign", "cmdNew", "cmdPruneStale", "cmdPlan"]
requires = [
  "io",
  "commands",
  "config",
  "declaration",
  "ownership",
  "crossings",
  "payload",
  "assign",
  "help",
]
layer = 7
