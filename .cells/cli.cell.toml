name = "cli"
purpose = "CLI entry: dispatch commands; wire the io layer + logic cells to argv."
provides = ["main"]
requires = [
  "io",
  "commands",
  "config",
  "declaration",
  "ownership",
  "crossings",
  "assign",
  "help",
]
layer = 7
