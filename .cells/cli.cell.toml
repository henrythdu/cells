name = "cli"
purpose = "CLI entry: dispatch commands; wire the io layer + logic cells to argv."
provides = ["main"]
requires = [
  "io",
  "declaration",
  "ownership",
  "payload",
  "validate",
  "crossings",
  "view",
  "assign",
  "structure",
  "help",
]

layer = "detail"
