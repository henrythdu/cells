name = "commands"
purpose = "Read/analysis command handlers (crossings, list, show, graph, owns, payload, surface) — the payload-shaped half of the CLI. The shared read pipeline (loadCrossings + guards) lives in the pipeline cell; the gate family (health/size/structure/impact) lives in the gate cell."
provides = [
  "cmdCrossings",
  "cmdList",
  "cmdShow",
  "cmdGraph",
  "cmdOwns",
  "cmdPayload",
  "cmdSurface",
]
requires = [
  "io",
  "crossings",
  "view",
  "graph",
  "diff",
  "pipeline",
  "payload",
  "declaration",
  "ownership",
  "validate",
]
layer = 6
