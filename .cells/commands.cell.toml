name = "commands"
purpose = "Read/analysis command handlers (crossings, imports, list, show, graph, owns, payload, surface) + the shared read pipeline (loadCrossings, requireCell, warnIfNoCodeFiles) — the payload-shaped half of the CLI. The gate family (health/size/structure/impact) lives in the gate cell."
provides = [
  "cmdCrossings",
  "cmdImports",
  "cmdList",
  "cmdShow",
  "cmdGraph",
  "cmdOwns",
  "cmdPayload",
  "cmdSurface",
  "loadCrossings",
  "requireCell",
  "warnIfNoCodeFiles",
]
requires = [
  "io",
  "crossings",
  "view",
  "graph",
  "diff",
  "importers",
  "imports",
  "payload",
  "declaration",
  "config",
  "ownership",
  "validate",
]
layer = 6
