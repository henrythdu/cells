name = "config"
purpose = "Parse project settings (.cells/config.toml); holds the payload-size ceiling default + the `cells init` template (DEFAULT_CONFIG)."
provides = ["parseConfig", "DEFAULT_MAX_PAYLOAD_TOKENS", "DEFAULT_CONFIG"]
requires = []

layer = 0
